const fs = require('fs');
const path = require('path');
const crypto = require('crypto');

const root = process.cwd();
const checkOnly = process.argv.includes('--check');
const idPattern = /^OS-TH-[0-9]{4}$/;
const siteBase = 'https://edulinked-coder.github.io/Osteosarcoma-Therapeutic-Hypotheses-Open-Machine-Readable-Research/';
let stale = false;

const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const safeExternalUrl = (value) => {
  const url = new URL(String(value));
  if (!['http:', 'https:'].includes(url.protocol)) throw new Error('Unsupported public source URL scheme: ' + url.protocol);
  return esc(url.toString());
};
const readText = (rel) => fs.readFileSync(path.join(root, rel), 'utf8');
const readJson = (rel) => JSON.parse(readText(rel));
const writeProjection = (rel, content) => {
  const target = path.join(root, rel);
  const normalized = content.endsWith('\n') ? content : content + '\n';
  if (checkOnly) {
    if (!fs.existsSync(target) || fs.readFileSync(target, 'utf8') !== normalized) {
      console.error('STALE GENERATED PROJECTION: ' + rel);
      stale = true;
    }
    return;
  }
  fs.mkdirSync(path.dirname(target), { recursive: true });
  fs.writeFileSync(target, normalized, 'utf8');
  console.log('Generated ' + rel);
};

const hypothesisDirs = fs.readdirSync(path.join(root, 'hypotheses'), { withFileTypes: true })
  .filter((entry) => entry.isDirectory() && idPattern.test(entry.name))
  .map((entry) => entry.name)
  .sort();

if (hypothesisDirs.length === 0) throw new Error('No canonical hypothesis directories found.');

const records = hypothesisDirs.map((id) => {
  const rel = `hypotheses/${id}/hypothesis.json`;
  const raw = readText(rel);
  const object = JSON.parse(raw);
  if (object.hypothesis_id !== id) throw new Error(`${rel} hypothesis_id does not match its stable directory.`);
  const digest = crypto.createHash('sha256').update(JSON.stringify(object)).digest('hex');
  return { id, rel, raw, object, digest };
});

const evidencePathsFor = (h) => {
  const refs = [...(h.supporting_evidence || []), ...((h.contradictory_evidence || {}).items || [])]
    .map((item) => item.evidence_id)
    .filter(Boolean)
    .map((id) => `evidence-bindings/${id}.json`);
  return [...new Set(refs)].sort();
};

const latestUpdatedAt = records.map(({ object }) => object.provenance.updated_at).sort().at(-1);
const index = {
  schema_version: '1.0.0',
  generated_by: 'scripts/render-public-research.js',
  generated_at: latestUpdatedAt,
  source: 'Canonical public hypothesis objects in /hypotheses/. Generated by scripts/render-public-research.js.',
  clinical_use_boundary: 'Research hypotheses only. Not medical advice, treatment recommendations, dosing instructions or claims of patient benefit.',
  hypothesis_count: records.length,
  hypotheses_requiring_review: records.filter(({ object }) => object.review_state.scientific_review_required && object.review_state.status !== 'reviewed').length,
  hypotheses: records.map(({ object: h }) => ({
    schema_version: h.schema_version,
    hypothesis_id: h.hypothesis_id,
    title: h.title,
    plain_language_summary: h.plain_language_summary,
    hypothesis_statement: h.hypothesis_statement,
    evidence_stage: h.evidence_stage,
    review_state: h.review_state,
    publication_path: `hypotheses/${h.hypothesis_id}/`,
    canonical_object_path: `hypotheses/${h.hypothesis_id}/hypothesis.json`,
    evidence_binding_paths: evidencePathsFor(h),
    last_evidence_update: h.provenance.updated_at,
    clinical_use: h.clinical_use
  }))
};
writeProjection('indexes/hypotheses.json', JSON.stringify(index, null, 2));

const style = `:root{--text:#172033;--muted:#42526e;--surface:#f7f5ff;--border:#c9c3e6;--gold:#dea93f;--radius:1rem;--shadow:0 10px 30px rgba(0,0,0,.08)}*{box-sizing:border-box}body{margin:0;color:var(--text);font-family:system-ui,-apple-system,"Segoe UI",sans-serif;font-size:1.0625rem;line-height:1.6}.container{width:min(100% - 2rem,80rem);margin:auto}.hero{padding:4rem 0;background:linear-gradient(135deg,#fff,#f7f5ff 35%,#fff7e8 70%,#edf8ff)}.hero-grid{display:grid;grid-template-columns:minmax(0,1.4fr) minmax(18rem,.8fr);gap:3rem;align-items:center}.eyebrow{font-weight:900;letter-spacing:.04em;text-transform:uppercase;color:var(--muted)}h1{font-size:clamp(2.3rem,7vw,4rem);line-height:1.1;margin:.5rem 0;max-width:13ch}.gradient{background:linear-gradient(90deg,#8e44ad,#d6336c,#f39c12,#2ecc71,#3498db);background-clip:text;-webkit-background-clip:text;color:transparent}.lead{font-size:1.2rem;color:var(--muted);max-width:68ch}.hero-card,.card,.table-wrap{border:1px solid var(--border);border-radius:var(--radius);background:#fff;box-shadow:var(--shadow)}.hero-card{padding:2rem;background:linear-gradient(135deg,rgba(142,68,173,.14),rgba(214,51,108,.11),rgba(243,156,18,.14),rgba(46,204,113,.10),rgba(52,152,219,.14))}.hero-inner{background:#fff;border-radius:var(--radius);padding:2rem}.line{height:8px;background:linear-gradient(90deg,#8e44ad,#d6336c,#f39c12,#2ecc71,#3498db);border-radius:999px}.metrics{display:grid;grid-template-columns:repeat(2,1fr);gap:1rem;margin-top:1rem}.metric{border-left:6px solid #c0b8cc;padding:1rem;background:var(--surface);border-radius:.75rem}.metric strong{display:block;font-size:2rem}.section{padding:4rem 0}.muted{background:var(--surface)}h2{font-size:clamp(1.65rem,4vw,2.35rem);line-height:1.2}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(100%,24rem),1fr));gap:1.5rem;margin-top:2rem}.card{padding:1.5rem;display:flex;flex-direction:column;gap:1rem;border-left:6px solid var(--gold)}.identity{display:flex;gap:1rem;align-items:center}.icon{width:5rem;height:5rem;flex:0 0 5rem;display:grid;place-items:center;border-radius:1rem;background:linear-gradient(135deg,#f7f5ff,#edf8ff,#fff7e8);font-size:1.2rem;font-weight:900}.kicker{margin:0;color:var(--muted);font-size:.92rem;font-weight:900;text-transform:uppercase}.title{margin:.15rem 0;font-size:1.35rem}.chips,.actions,.docs{display:flex;gap:.5rem;flex-wrap:wrap}.chip,.doc{padding:.35rem .7rem;border-radius:999px;border:1px solid var(--border);background:var(--surface);font-weight:800;font-size:.9rem}.doc{border-radius:.7rem}.button{display:inline-flex;min-height:44px;align-items:center;padding:.7rem 1rem;border-radius:999px;text-decoration:none;font-weight:900;background:#222830;color:#fff}.primary{background:linear-gradient(90deg,#8e44ad,#d6336c,#f39c12)}.steps{display:grid;grid-template-columns:repeat(auto-fit,minmax(10rem,1fr));gap:1rem;margin-top:2rem}.step{background:#fff;padding:1.25rem;border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow);border-top:6px solid #c0b8cc}.callout{border-left:6px solid var(--gold);background:#fff9ec;padding:1.5rem;border-radius:.75rem;margin-top:2rem}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse;min-width:45rem}th,td{padding:.9rem 1rem;border-bottom:1px solid var(--border);text-align:left;vertical-align:top}th{background:var(--surface)}.bottom{padding:4rem 0;background:#222830;color:#fff;text-align:center}@media(max-width:900px){.hero-grid{grid-template-columns:1fr}h1{max-width:none}}`;

const cards = records.map(({ object: h }) => `<article class="card"><div class="identity"><div class="icon">${esc(h.hypothesis_id.slice(-4))}</div><div><p class="kicker">${esc(h.review_state.publication_class)}</p><h3 class="title">${esc(h.title)}</h3></div></div><div class="chips"><span class="chip">${esc(h.hypothesis_id)}</span><span class="chip">${esc(h.evidence_stage)}</span><span class="chip">uncertainty: ${esc(h.uncertainty.level)}</span></div><p>${esc(h.plain_language_summary)}</p><div class="actions"><a class="button primary" href="hypotheses/${esc(h.hypothesis_id)}/">Open hypothesis</a><a class="button" href="hypotheses/${esc(h.hypothesis_id)}/hypothesis.json">JSON</a></div><div class="docs"><span class="doc">Evidence: ${(h.supporting_evidence || []).length}</span><span class="doc">Contradictions: ${esc(h.contradictory_evidence.status)}</span><span class="doc">Ranking: ${h.ranking.score === null ? 'pending' : esc(h.ranking.score)}</span></div></article>`).join('');
const registerRows = records.map(({ object: h }) => `<tr><td><a href="hypotheses/${esc(h.hypothesis_id)}/">${esc(h.hypothesis_id)}</a></td><td>${esc(h.title)}</td><td>${esc(h.evidence_stage)}</td><td>${esc(h.review_state.publication_class)}</td><td>${esc(h.review_state.status)}</td></tr>`).join('');
const reviewCount = index.hypotheses_requiring_review;
const supersededCount = records.filter(({ object }) => object.supersession.status === 'superseded').length;
const lastUpdate = latestUpdatedAt.slice(0, 10);
const homepage = `<!doctype html>
<!-- GENERATED by scripts/render-public-research.js. Edit canonical hypothesis JSON, not generated scientific content here. -->
<html lang="en-AU"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Osteosarcoma Research Portfolio</title><meta name="description" content="Open, evidence-bound osteosarcoma therapeutic research hypotheses for human and machine discovery."><style>${style}</style></head><body><main><section class="hero"><div class="container hero-grid"><div><p class="eyebrow">Open research · Osteosarcoma</p><h1>Therapeutic <span class="gradient">hypotheses</span></h1><p class="lead"><strong>Find research ideas you can recognise and inspect.</strong> Every card on this site is generated from a canonical machine-readable research object.</p><div class="actions"><a class="button primary" href="#hypotheses">Explore hypotheses</a><a class="button" href="https://github.com/EduLinked-coder/Osteosarcoma-Therapeutic-Hypotheses-Open-Machine-Readable-Research">Source repository</a></div></div><div class="hero-card"><div class="hero-inner"><h2>Research at a glance</h2><div class="line"></div><div class="metrics"><div class="metric"><strong>${records.length}</strong>Public objects</div><div class="metric"><strong>${reviewCount}</strong>Need review</div><div class="metric"><strong>${supersededCount}</strong>Superseded</div><div class="metric"><strong>${esc(lastUpdate)}</strong>Last update</div></div></div></div></div></section><section class="section" id="hypotheses"><div class="container"><p class="eyebrow">Find a hypothesis</p><h2>Canonical public research objects</h2><p class="lead">Human-facing cards are generated from the same canonical JSON used by machines. The website does not maintain a separate hypothesis record.</p><div class="grid">${cards}</div><div class="callout"><strong>Research boundary:</strong> these are research hypotheses, not established treatments, dose instructions, clinical recommendations or claims of patient benefit.</div></div></section><section class="section muted"><div class="container"><p class="eyebrow">How the system works</p><h2>Research operating sequence</h2><div class="steps"><div class="step"><strong>1 Discover</strong><br>Find current evidence.</div><div class="step"><strong>2 Bind</strong><br>Keep evidence attached to source.</div><div class="step"><strong>3 Generate</strong><br>Create testable hypotheses.</div><div class="step"><strong>4 Rank</strong><br>Prioritise research questions.</div><div class="step"><strong>5 Validate</strong><br>Check evidence and contradictions.</div><div class="step"><strong>6 Privacy gate</strong><br>Exclude private context.</div><div class="step"><strong>7 Review</strong><br>Preserve scientific authority.</div><div class="step"><strong>8 Publish</strong><br>Project for people and machines.</div></div></div></section><section class="section"><div class="container"><p class="eyebrow">Research register</p><h2>Public hypothesis lifecycle</h2><div class="table-wrap"><table><thead><tr><th>Stable ID</th><th>Research object</th><th>Evidence stage</th><th>Public class</th><th>Review state</th></tr></thead><tbody>${registerRows}</tbody></table></div><div class="callout"><strong>Revision rule:</strong> new evidence can rerank, supersede or withdraw an object while preserving provenance and history.</div></div></section><section class="bottom"><div class="container"><h2>Open research. Evidence attached.</h2><p>People and autonomous research systems can inspect the same public research state.</p></div></section></main></body></html>`;
writeProjection('index.html', homepage);

const pageStyle = `:root{--text:#172033;--muted:#42526e;--surface:#f7f5ff;--border:#c9c3e6;--gold:#dea93f}*{box-sizing:border-box}body{margin:0;font-family:system-ui,-apple-system,"Segoe UI",sans-serif;color:var(--text);line-height:1.7}main{width:min(100% - 2rem,70rem);margin:auto;padding:3rem 0}header{padding:2.5rem;border-radius:1rem;background:linear-gradient(135deg,#fff,#f7f5ff,#fff7e8,#edf8ff);border:1px solid var(--border)}h1{font-size:clamp(2rem,6vw,3.5rem);line-height:1.1}.pill{display:inline-block;padding:.25rem .65rem;border:1px solid var(--border);border-radius:999px;background:var(--surface);font-weight:700;margin:.15rem}.warning{border-left:6px solid var(--gold);background:#fff9ec;padding:1.2rem;margin:2rem 0;border-radius:.6rem}section{border-top:1px solid var(--border);margin-top:2rem;padding-top:.5rem}li{margin:.65rem 0}a{color:#2349a6}.meta{color:var(--muted);font-size:.95rem}`;
const list = (items) => (items || []).map((item) => `<li>${esc(item)}</li>`).join('');

for (const { object: h, digest } of records) {
  const evidence = (h.supporting_evidence || []).map((item) => `<li><a href="${safeExternalUrl(item.source_url)}">${esc(item.citation)}</a><br>${esc(item.finding)}<br><a href="../../evidence-bindings/${esc(item.evidence_id)}.json">Public evidence binding</a></li>`).join('');
  const contradictions = ((h.contradictory_evidence || {}).items || []).length
    ? `<ul>${h.contradictory_evidence.items.map((item) => `<li>${esc(item.finding || item.citation || item.evidence_id)}</li>`).join('')}</ul>`
    : '';
  const validation = (h.validation_requirements || []).map((item) => `<li><strong>${esc(item.stage)}</strong> — ${esc(item.requirement)} <span class="pill">${esc(item.status)}</span></li>`).join('');
  const score = h.ranking.score === null ? 'Pending recalculation' : `${esc(h.ranking.score)}/100`;
  const page = `<!doctype html>
<!-- GENERATED by scripts/render-public-research.js. Canonical source: hypothesis.json -->
<html lang="en-AU"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(h.hypothesis_id)} | ${esc(h.title)}</title><meta name="canonical-object-digest" content="sha256:${digest}"><meta name="description" content="${esc(h.plain_language_summary)}"><style>${pageStyle}</style></head><body><main><header><p><strong>${esc(h.hypothesis_id)}</strong></p><h1>${esc(h.title)}</h1><p>${esc(h.plain_language_summary)}</p><span class="pill">${esc(h.evidence_stage)}</span><span class="pill">${esc(h.review_state.publication_class)}</span><span class="pill">clinical use: false</span></header><div class="warning"><strong>Research only.</strong> This is a testable research hypothesis, not an established treatment, medical advice, dose instruction or clinical recommendation.</div><section><h2>Hypothesis</h2><p>${esc(h.hypothesis_statement)}</p></section><section><h2>Why this hypothesis exists</h2><p>${esc(h.mechanism.summary)}</p></section><section><h2>Supporting evidence</h2><ul>${evidence}</ul></section><section><h2>Contradictory evidence</h2><p><strong>Status: ${esc(h.contradictory_evidence.status)}</strong></p><p>${esc(h.contradictory_evidence.assessment)}</p>${contradictions}</section><section><h2>Uncertainty</h2><p><strong>Level: ${esc(h.uncertainty.level)}</strong></p><p>${esc(h.uncertainty.summary)}</p><h3>Open questions</h3><ul>${list(h.uncertainty.open_questions)}</ul></section><section><h2>Research-priority ranking</h2><p><strong>${score}</strong></p><p>${esc(h.ranking.meaning)}</p></section><section><h2>Novelty</h2><p><strong>Status: ${esc(h.novelty.status)}</strong></p><p>${esc(h.novelty.assessment)}</p></section><section><h2>What would challenge or falsify it?</h2><ul>${list(h.falsifiability.falsification_conditions)}</ul><h3>Discriminating tests</h3><ul>${list(h.falsifiability.discriminating_tests)}</ul></section><section><h2>Validation still required</h2><ul>${validation}</ul></section><section><h2>Review and provenance</h2><p>Review state: <strong>${esc(h.review_state.status)}</strong>. Scientific review required: <strong>${esc(h.review_state.scientific_review_required)}</strong>.</p><p>Source authority: ${esc(h.provenance.source_authority)}</p><p class="meta">Canonical object updated: ${esc(h.provenance.updated_at)} · Projection digest: sha256:${digest}</p><p><a href="hypothesis.json">Canonical JSON</a> · <a href="../../indexes/hypotheses.json">Machine index</a> · <a href="../../">Research portfolio</a></p></section></main></body></html>`;
  writeProjection(`hypotheses/${h.hypothesis_id}/index.html`, page);
}

const sitemapEntries = [
  `  <url>\n    <loc>${siteBase}</loc>\n    <lastmod>${latestUpdatedAt.slice(0, 10)}</lastmod>\n  </url>`,
  ...records.map(({ object: h }) => `  <url>\n    <loc>${siteBase}hypotheses/${h.hypothesis_id}/</loc>\n    <lastmod>${h.provenance.updated_at.slice(0, 10)}</lastmod>\n  </url>`)
];
writeProjection('sitemap.xml', `<?xml version="1.0" encoding="UTF-8"?>\n<!-- GENERATED by scripts/render-public-research.js -->\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${sitemapEntries.join('\n')}\n</urlset>`);

if (checkOnly && stale) process.exitCode = 1;
if (checkOnly && !stale) console.log(`Generated projections are current for ${records.length} hypothesis object(s).`);
